<%= form_for(@repeat) do |f| %>
  <% if @repeat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@repeat.errors.count, "error") %> prohibited this repeat from being saved:</h2>

      <ul>
      <% @repeat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
